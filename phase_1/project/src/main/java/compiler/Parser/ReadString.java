package compiler.Parser;

import compiler.Lexer.Identifier;

import java.util.ArrayList;

public class ReadString extends ProcedureIO {

    public ReadString(Identifier identifier, ArrayList<ArrayOfExpression> params) {
        super(identifier, params);
    }
}
