package compiler.Parser;

import compiler.Lexer.Identifier;

import java.util.ArrayList;

public class ReadReal extends ProcedureIO {

    public ReadReal(Identifier identifier, ArrayList<ArrayOfExpression> params) {
        super(identifier, params);
    }
}
